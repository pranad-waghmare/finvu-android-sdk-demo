<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DemoApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Finvu"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activities.ProcessConsentActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LinkedAccountActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AccountLinkingConfirmActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PopularSearchActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DiscoverActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
